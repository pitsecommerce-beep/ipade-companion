# Este workflow ya no es necesario.
# Las Edge Functions de Supabase fueron reemplazadas por rutas Express
# en el servidor Railway (server/routes/). Las variables de entorno
# (ANTHROPIC_API_KEY, RESEND_API_KEY, etc.) se configuran directamente
# en el dashboard de Railway → Variables.
#
# Conservado como referencia. Puedes eliminarlo si lo deseas.
